# Makefile.in for BASIC compiler
VPATH=@srcdir@
srcdir=@srcdir@

SUBDIRS=basic pbc

all clean install:
	for subdir in $(SUBDIRS); do \
		(cd $$subdir && $(MAKE) $@) || exit 1; \
	done

.PHONY: all clean install
