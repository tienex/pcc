; Test SOSGE - Subtract One from memory and Skip if Greater or Equal (PDP-10)
; Expected: DATA--, skip if result >= 0

	.PSECT	CODE

START:
	MOVEI 1,1
	MOVEM 1,DATA
	SOSGE	DATA
	HALT

DATA:	.WORD	0

	.END
