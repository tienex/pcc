; Test CAMG - Compare AC with Memory, skip if Greater (PDP-10)
; Expected: Compare AC1 with DATA, skip if AC1 > DATA

	.PSECT	CODE

START:
	MOVEI 1,100
	MOVEI 2,50
	MOVEM 2,DATA
	CAMG	1,DATA
	HALT

DATA:	.WORD	0

	.END
