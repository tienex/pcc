#	$Id$
#
# Makefile.in for libgc - Generic garbage collector
#
VPATH=@srcdir@
srcdir=@srcdir@
top_srcdir=@top_srcdir@
top_builddir=@top_builddir@
CC = @CC@
AR = ar
RANLIB = @RANLIB@
CFLAGS = @CFLAGS@ @ADD_CFLAGS@
CPPFLAGS = @CPPFLAGS@ @ADD_CPPFLAGS@ -I$(srcdir) -I$(top_builddir)
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@

prefix = @prefix@
exec_prefix = @exec_prefix@
libdir = @libdir@
includedir = @includedir@

OBJS=	gc.o gc_compact.o gc_pool.o gc_weak.o

LIB=libgc.a

all: $(LIB)

$(LIB): $(OBJS)
	$(AR) rv $@ $(OBJS)
	$(RANLIB) $@

gc.o: $(srcdir)/gc.c $(srcdir)/gc.h $(srcdir)/gc_internal.h
	$(CC) $(CFLAGS) $(CPPFLAGS) -c $(srcdir)/gc.c

gc_compact.o: $(srcdir)/gc_compact.c $(srcdir)/gc.h
	$(CC) $(CFLAGS) $(CPPFLAGS) -c $(srcdir)/gc_compact.c

gc_pool.o: $(srcdir)/gc_pool.c $(srcdir)/gc.h $(srcdir)/gc_internal.h
	$(CC) $(CFLAGS) $(CPPFLAGS) -c $(srcdir)/gc_pool.c

gc_weak.o: $(srcdir)/gc_weak.c $(srcdir)/gc.h $(srcdir)/gc_internal.h
	$(CC) $(CFLAGS) $(CPPFLAGS) -c $(srcdir)/gc_weak.c

install: $(LIB)
	test -z "$(DESTDIR)$(libdir)" || mkdir -p "$(DESTDIR)$(libdir)"
	test -z "$(DESTDIR)$(includedir)" || mkdir -p "$(DESTDIR)$(includedir)"
	$(INSTALL_DATA) $(LIB) $(DESTDIR)$(libdir)
	$(INSTALL_DATA) $(srcdir)/gc.h $(DESTDIR)$(includedir)

clean:
	rm -f $(OBJS) $(LIB)

distclean: clean
	rm -f Makefile
