# Makefile for C# Compiler

VPATH = @srcdir@
srcdir = @srcdir@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

SUBDIRS = cscom csharp

all clean install:
	@for dir in $(SUBDIRS); do \
		(cd $$dir && $(MAKE) $@) || exit 1; \
	done

.PHONY: all clean install
